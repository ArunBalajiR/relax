import 'package:flutter/material.dart';

const List<BoxShadow> kInactiveCardDecoration = [
  BoxShadow(
      color: Color(0xFF5bc6ff),
      offset: Offset(5.0, 5.0),
      blurRadius: 15.0,
      spreadRadius: 1.0),
  BoxShadow(
      color: Color(0xFF4da7db),
      offset: Offset(-5.0, -5.0),
      blurRadius: 15.0,
      spreadRadius: 1.0),
];

const List<BoxShadow> kActiveCardDecoration = null;

const Color kBlueShade1 = Color(0xFF4ca5d8);
const Color kBlueShade2 = Color(0xFF5ecdff);

const Color kPrimaryBlue = Color(0xFF55b9f3);
const Color kDarkBlue = Color(0xFF001E3D);
